import 'package:flutter/material.dart';

class BodyOrderProperView extends StatelessWidget {
  const BodyOrderProperView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Your order has been processed correctly.");
  }
}